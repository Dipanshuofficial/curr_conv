// import { getCurrencies } from "@/constants/ApiHandler";

import DataGridDemo from "./subComps/table";
const Rates = () => {
  return (
    <div className={`flex items-center justify-center`}>
      <DataGridDemo />
    </div>
  );
};

export default Rates;
